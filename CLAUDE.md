# Claurst Claude Memory

Read `AGENTS.md` first. It is the canonical repo-local agent memory for this fork and includes:

- current Codex OAuth / `/fast` semantics,
- RTK command/integration notes,
- TUI clipboard/paste/theme/settings/memory fixes,
- graphify instructions,
- verification commands,
- next tasks.

Critical local rule: do not commit unless explicitly asked; when committing, use `xenking <support@xenking.pro>`.
